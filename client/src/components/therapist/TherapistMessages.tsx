import { Card, CardHeader, CardTitle, CardContent } from '../ui/card';

export function TherapistMessages() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Messages</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Messages section - Communicate with parents and clinicians</p>
      </CardContent>
    </Card>
  );
}
