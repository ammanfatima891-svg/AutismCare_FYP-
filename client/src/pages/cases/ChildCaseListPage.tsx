/**
 * Route-level entry for Child Case list (re-uses shared component).
 * Main integration lives in ClinicianDashboard → Child Cases.
 */
export { ChildCaseList as default } from '../../components/case/ChildCaseList';
